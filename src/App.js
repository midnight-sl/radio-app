import React, {Component} from "react";
import { hot } from "react-hot-loader";
import "./App.js";

class App extends Component {
  render(){
    return(
      <div className="App">
        <h1>Radio will be here soon</h1>
      </div>
    );
  }
}

export default hot(module)(App);
